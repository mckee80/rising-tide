---
import BaseLayout from '@/layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
  (a, b) => new Date(b.data.pubDate).valueOf() - new Date(a.data.pubDate).valueOf()
);

const base = import.meta.env.BASE_URL;
---

<BaseLayout
  title="Blog | Timber Framing Articles & Insights"
  description="Articles about timber frame construction, wood species, joinery techniques, design inspiration, and the craft of building heirloom timber structures."
  breadcrumbs={[{ name: 'Blog', url: '/blog/' }]}
>
  <!-- Hero -->
  <section class="pt-36 pb-16 bg-gradient-to-br from-brand-primary/10 to-transparent">
    <div class="container-max">
      <div class="max-w-3xl">
        <span class="section-label">Blog</span>
        <h1 class="text-4xl md:text-5xl mb-4">Timber Framing Insights</h1>
        <p class="text-lg text-brand-dark-light">
          Articles about timber frame construction, craftsmanship, wood species, design inspiration, and the art of building structures that last for generations.
        </p>
      </div>
    </div>
  </section>

  <!-- Posts Grid -->
  <section class="section-padding">
    <div class="container-max">
      {posts.length === 0 ? (
        <div class="text-center py-12">
          <p class="text-brand-dark-light text-lg">No posts yet. Check back soon!</p>
        </div>
      ) : (
        <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8">
          {posts.map((post) => (
            <article class="bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-lg transition-shadow border border-surface-alt">
              {post.data.image && (
                <img
                  src={post.data.image}
                  alt={post.data.title}
                  class="w-full h-48 object-cover"
                  loading="lazy"
                />
              )}
              <div class="p-6">
                <div class="flex flex-wrap gap-2 mb-3">
                  {post.data.tags?.slice(0, 2).map((tag) => (
                    <span class="text-xs font-medium uppercase tracking-wider text-brand-primary bg-brand-primary/10 px-2 py-0.5 rounded-full">
                      {tag}
                    </span>
                  ))}
                </div>
                <h2 class="text-xl font-heading mb-2">
                  <a href={`${base}blog/${post.slug}/`} class="text-brand-dark hover:text-brand-primary transition-colors">
                    {post.data.title}
                  </a>
                </h2>
                <p class="text-brand-dark-light text-sm mb-4 line-clamp-2">
                  {post.data.description}
                </p>
                <div class="flex justify-between items-center text-sm text-brand-dark-light">
                  <time datetime={post.data.pubDate.toISOString()}>
                    {new Date(post.data.pubDate).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })}
                  </time>
                  <a href={`${base}blog/${post.slug}/`} class="text-brand-primary hover:text-brand-primary-dark font-medium transition-colors">
                    Read more →
                  </a>
                </div>
              </div>
            </article>
          ))}
        </div>
      )}
    </div>
  </section>
</BaseLayout>
